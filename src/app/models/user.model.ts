export class User {
  constructor(username: any, email: any, name: any) {
    this.username = username;
    this.email = email;
    this.name = name;
  }

  username: String
  email: String
  name: String
}
